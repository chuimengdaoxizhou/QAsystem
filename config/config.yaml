app:
  name: "qasystem"
  environment: "development"
  port: "8080"

jwt:
  secret_key: "qasystem"


database:
  driver: mysql
  host: localhost
  port: 3306
  user: root
  password: mysql
  dbname: QASystem
  charset: utf8mb4
  parseTime: true
  loc: Local
